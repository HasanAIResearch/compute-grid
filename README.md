# AI Infrastructure Acceleration

Public GitHub Pages draft.

## Publish
Create a public GitHub repository named `ai-infra-acceleration`, push this folder to `main`, then enable GitHub Pages from Settings -> Pages -> Deploy from branch -> main / root.

Expected free URL:
`https://<your-github-username>.github.io/ai-infra-acceleration/`
